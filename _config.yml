title: GitHub Admin Training — GitHub Enterprise Cloud (L300)
description: Advanced 2-day workshop for GitHub Enterprise Cloud administration, governance, and best practices
theme: jekyll-theme-architect

# Build settings
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Include these directories
include:
  - docs
  - labs

# Collections
collections:
  docs:
    output: true
    permalink: /docs/:path/
  labs:
    output: true
    permalink: /labs/:path/

# Defaults
defaults:
  - scope:
      path: ""
      type: "docs"
    values:
      layout: default
  - scope:
      path: ""
      type: "labs"
    values:
      layout: default

# Exclude from processing
exclude:
  - README.md
  - LICENSE
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
